distCheckR<-function(...){
  # function to convert input into a matrix of equal width and length, repeating values as necessary
  # this does not work! ! ! !
  
  # returning value if only one value has been inserted
  if (nargs() == 1){
  return(...)}
  # seperating arguments intor a list
  arguments<-list(...)

    n = nargs()
    leng<-lengths(arguments)
    t = zeros(size1);
  

  return(newVec)}
